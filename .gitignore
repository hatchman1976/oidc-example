# .gitignore for Terraform

# Local .terraform directories
.terraform/

# Terraform state files
*.tfstate
*.tfstate.*

# Crash logs
crash.log

# Ignore sensitive Terraform variables files
*.tfvars
*.tfvars.json

# Terraform plan files
*.tfplan

# Ignore override files (used for local-only changes)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# .terraform.lock.hcl can be ignored if you're using a different lock mechanism
# but it's recommended to keep it in version control to ensure consistent provider versions
# .terraform.lock.hcl

# CLI-specific config files
.terraformrc
terraform.rc
